/**
 * Stable scene import. Procedural texture generation lives in the asset
 * infrastructure layer so scene composition stays separate from asset setup.
 */
export { ProceduralAssetScene as BootScene } from '../infrastructure/assets/ProceduralAssetScene';
